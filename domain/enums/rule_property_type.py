"""
RulePropertyType Enum
"""
from enum import Enum


class RulePropertyType(Enum):
    """
    Enum representing the types of properties that can be used in a rule.
    """
    INDICATOR = 'INDICATOR'
